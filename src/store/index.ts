import Vue from 'vue'
import Vuex from 'vuex'
// import pokemons from '@/store/modules/pokemons'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    // pokemons
  }
})
