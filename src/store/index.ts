import Vue from 'vue'
import Vuex from 'vuex'
import pokes from '@/store/modules/pokes.ts'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    pokes
  }
})
