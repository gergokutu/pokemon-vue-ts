export interface IPokeCard {
  name: string;
  url: string;
}

export interface Detail {
  data: object;
}

export interface PokeType {
  name: string;
  url: string;
}

//
export interface PokeName {
  pokemon: object;
  slot: number;
}